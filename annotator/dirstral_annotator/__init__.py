"""dirstral-annotator: the reference recognition backend for dir2mcp's
`recognize` capability (dirstral-spec design 0004). `dirstral-annotate
serve` answers dir2mcp's `POST /recognize` calls with time-coded,
confidence-scored annotation statements that dir2mcp persists as a derived
representation and indexes into searchable, citable moments.
"""

__version__ = "0.2.0"
