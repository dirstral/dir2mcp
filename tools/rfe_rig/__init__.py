"""Cross-model agreement rig for the RFE validation corpus (dir2mcp #1032).

Pure stdlib. The only non-stdlib code is the MMS helper, which runs in a
separate interpreter that has torch and transformers installed.
"""
